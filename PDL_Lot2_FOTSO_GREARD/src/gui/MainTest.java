package gui;

import dao.*;
import model.*;

public class MainTest {
	public static void main(String [] args) {
		
		/*System.out.println("START");		
		
		DatePerso date = new DatePerso(07,06,2001);
		PersonneMorale pm = new PersonneMorale("GREARD", "GUILLAUME", date);
		Participant p1 = new Participant("TruelleEnInox", "Azerty123", pm);
		Delegation d1 = new Delegation(p1.getIdentifiant(), p1.getMdp(), "France", 50, pm);
		
		DatePersoDAO dateDAO = new DatePersoDAO();
		PersonneMoraleDAO pmDAO = new PersonneMoraleDAO();
		ParticipantDAO pDAO = new ParticipantDAO();
		DelegationDAO dDAO = new DelegationDAO();
		
		
		pDAO.add(p1,(pmDAO.add(pm, dateDAO.add(date))));
		pDAO.update(p1, "IDDELEGATION", dDAO.add(d1));
		
		
		System.out.println("END");*/
		
	}
}
